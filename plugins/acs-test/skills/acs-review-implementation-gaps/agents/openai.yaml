interface:
  display_name: "Review Implementation Gaps"
  short_description: "Compare the current codebase against a design doc (DESIGN.md, docs/plans/*.md) a..."
policy:
  allow_implicit_invocation: false
