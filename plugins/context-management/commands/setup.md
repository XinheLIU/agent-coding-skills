---
description: Initialize or synchronize the repository's shared agent context.
argument-hint: "[optional preferences]"
---

Last updated: 2026-09-17

Use `acs-init-context` when `docs/agents/memory.md` is absent. Otherwise use `acs-sync-context`. Pass `$ARGUMENTS` as preferences. Present the proposed writes for approval before changing the repository.
