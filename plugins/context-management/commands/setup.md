---
description: Initialize or synchronize the repository's shared agent context.
argument-hint: "[optional preferences]"
---

Last updated: 2026-08-25

Use `init-context` when `docs/agents/memory.md` is absent. Otherwise use `sync-context`. Pass `$ARGUMENTS` as preferences. Present the proposed writes for approval before changing the repository.
