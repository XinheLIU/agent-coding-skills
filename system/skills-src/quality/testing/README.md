# Quality · Testing

Last updated: 2026-09-17

The skills from this sub-phase have been relocated.

| Public skill | Canonical source |
| --- | --- |
| `acs-tdd` | [Build](../../build/acs-tdd/SKILL.md) |
| `acs-analyze-test-gaps` | [Test](../../test/acs-analyze-test-gaps/SKILL.md) |

The obsolete source copies and broken nested symlinks were removed during the ACS namespace migration.

`acs-tdd` moved to `build/` because test-driven development is part of the implementation loop, not a post-build quality step. `acs-analyze-test-gaps` moved to `test/` because coverage audits and integration test gaps are addressed after build is complete.

See [`build/README.md`](../../build/README.md) and [`workflows/test.md`](../../../workflows/test.md).
