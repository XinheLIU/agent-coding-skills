---
description: Configure the shared memory protocol for this repository
argument-hint: "[optional: tracker, work-root, or recovery-entry preference]"
---

# Setup Agent Coding System

Last updated: 2026-09-25

Use the `acs-init-context` skill when routing needs configuration. If `docs/agents/memory.md` already exists, use `acs-sync-context` for drift. Pass `$ARGUMENTS` as preferences, inspect existing homes, and configure Working/Persistent Memory with one recovery entry per run. Apply authorized setup; ask only for unresolved choices or additional authorization. Use the Presenter only when a configuration comparison aids review. Do not install dependencies or create empty memory artifacts.
